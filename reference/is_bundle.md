# Check whether an object is a bundle

Check whether an object is a bundle

## Usage

``` r
is_bundle(x)
```

## Arguments

- x:

  An object.

## Value

`TRUE` if `x` inherits from `"bundle"`, otherwise `FALSE`.
