# Check whether an object is a bundle

Check whether an object is a bundle

## Usage

``` r
is_bundle(x)
```

## Arguments

- x:

  An object.

## Value

`TRUE` if `x` is of class `<riot::bundle>`, otherwise `FALSE`.
