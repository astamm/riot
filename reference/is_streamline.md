# Check whether an object is a streamline

Check whether an object is a streamline

## Usage

``` r
is_streamline(x)
```

## Arguments

- x:

  An object.

## Value

`TRUE` if `x` inherits from `"streamline"`, otherwise `FALSE`.
