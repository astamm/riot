% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read-bundle.R
\name{read_bundle}
\alias{read_bundle}
\title{Import bundles into R}
\usage{
read_bundle(file, reference_file = NULL)
}
\arguments{
\item{file}{A string specifying the path to the file containing the tractography data. Currently
supported files are:
\itemize{
\item standard \href{https://vtk.org}{VTK} formats \code{.vtk} and \code{.vtp},
\item \href{https://med.inria.fr}{medInria} \code{.fds} format,
\item \href{https://mrtrix.readthedocs.io/en/latest/getting_started/image_data.html}{MRtrix} \code{.tck/.tsf} format,
\item \href{https://trackvis.org/docs/?subsect=fileformat}{TrackVis} \code{.trk} and \code{.trx} formats,
\item \href{https://docs.dipy.org/1.11.0/}{DIPY} \code{.dpy} format,
\item \code{.fib} format.
}}

\item{reference_file}{A string specifying the path to a reference image file. This is only required
when importing \code{.trx}, \code{.fib}, or \code{.dpy} files, as these formats do not contain spatial
information about the image space. The reference image is used to correctly position the
bundle in the appropriate space. Default is \code{NULL}.}
}
\value{
A \link[=new_bundle]{bundle} object when the file contains multiple
streamlines, or a \link[=new_streamline]{streamline} object when it contains
exactly one. Each \code{streamline} is a numeric matrix with at least three
named columns \code{"X"}, \code{"Y"}, and \code{"Z"} (one row per point along the
tract). Additional per-point scalar attributes, when present in the source
file, appear as extra named columns.
}
\description{
This is the go-to function to import bundles into R. Based on both VTK and DIPY, we currently
support eight different formats detailed in the documentation of input argument \code{file}.
}
\examples{
uf_left_vtk <- read_bundle(system.file("extdata", "UF_left.vtk",  package = "riot"))
}
\seealso{
\code{\link[=write_bundle]{write_bundle()}} to export bundles from R.
}
