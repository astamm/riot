CXX_STD = CXX11
PKG_CPPFLAGS = -Ivtk/include
PKG_LIBS = $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS) -Lvtk/lib -lvtk

VTK_OBJECTS = `ls vtk/lib/*`

all: clean vtklibs

vtklibs:
	  $(AR) -crvs ./vtk/lib/libvtk.a $(VTK_OBJECTS)
	  $(RANLIB) $@

clean:
	  rm -f $(SHLIB) $(OBJECTS) $(VTK_OBJECTS) *.dll *.exe
	  rm -fr vtk
